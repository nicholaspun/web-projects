{
  "name": "website",
  "version": "1.0.0",
  "description": "fun side project",
  "main": "webfun.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack",
    "start-dev": "webpack-dev-server --open --colors",
    "start-http-server": "node node_modules/http-server/bin/http-server --o"
  },
  "author": "Nicholas Pun",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^0.9.0"
  }
}
