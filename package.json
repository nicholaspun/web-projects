{
  "name": "temp-name",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://nicholaspun.github.io/500px-manager",
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "node-sass": "^4.5.1",
    "npm-run-all": "^4.0.2",
    "react-scripts": "0.9.5"
  },
  "scripts": {
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "start-js": "react-scripts start",
    "start": "npm-run-all -p watch-css start-js",
    "build": "npm run build-css && react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
